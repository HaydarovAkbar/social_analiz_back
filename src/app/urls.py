from django.urls import path
from . import views


urlpatterns = [
    # path('bot/', views.MainView.as_view(), name='bot'),
    # path('users/', views.UsersView.as_view(), name='users'),
    # path('login/', views.LogInView.as_view(), name='login'),
    # path('filter_by_date/', views.PiaFilterByDateView.as_view({'get': 'list'}), name='filter_by_date'),
    # path('graph_by_date/', views.GraphFilterByDateView.as_view({'get': 'list'}), name='graph_by_date'),
]
